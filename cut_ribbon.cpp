#include <bits./stdc++.h>
using namespace std;
int main()
{
    int n, a, b, c, m, count = 0, ram;
    cin >> n >> a >> b >> c;
    for(int i =0; i<n; i++){
        for(int j=0;j<n; j++){
           if( count = n-i*a+j*b){
               cout<<count<<endl;
           }
        }
    
       
    }

    return 0;
}